// 다음은 입출력장치로부터 양의 정수를 입력받아, 정수의 자릿수를 계산하는 C 프로그램이다. 다음 출력 화면을 참고하여, ①~②에 들어갈 알맞은 답을 작성하시오.

// 출력 화면
// 정수를 입력하시오.
// 12345
//
// 입력한 정수는 12345 입니다.
// 입력한 정수의 자릿수는 5 입니다.

#include <stdio.h>
int main() {
    int n, count = 0;
    printf("정수를 입력하시오.\n");
    ①
    printf("입력한 정수는 %d 입니다.\n", n);
    do {
        n = n / 10;
        count++;
    } while (②);
    printf("입력한 정수의 자릿수는 %d 입니다.\n", count);
    return 0;
}


// Solving
// 답 ① : scanf("%d", &n);
// 답 ② : n != 0
// 2번을 틀렸던 이유 : n > 0이라고 생각했음. 
// n > 0 → 0을 “종료 기준”으로는 보지 못함 (양수만 처리)
// n != 0 → 0이 될 때 정확히 멈춤 (0 자체도 세고 끝남)
// 12345 → 1234 → 123 → 12 → 1 → 0 (총 5번 나누기, count = 5)
// 자릿수 계산은 n이 0이 될 때까지 나누는 과정이므로 반복 조건은 “n ≠ 0”이 가장 자연스럽고 안전한 표현이다.